export declare const missingGtagMsg = "Gtag is missing. Add the `GoogleAnalytics` component to the `Head` component inside `_document.js`.";
declare type PageViewOptions = {
    title?: string;
    location?: string;
    path?: string;
    sendPageView?: boolean;
};
export declare function pageView({ title, location, path, sendPageView, }?: PageViewOptions): void;
declare type EventOptions = {
    category?: string;
    label?: string;
    value?: number;
    nonInteraction?: boolean;
};
export declare function event(action: string, { category, label, value, nonInteraction }?: EventOptions): void;
export {};
