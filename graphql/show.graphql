#import "./fragments/ShowFragment.graphql"
#import "./fragments/ShowFragmentFull.graphql"

query GetAllShows {
	shows: allShows(orderBy: [startDate_ASC]) {
		...ShowFragment
	}
}
query GetShow($slug:String!) {
	show(filter: {slug: {eq: $slug}}) {
		...ShowFragmentFull
	}
}