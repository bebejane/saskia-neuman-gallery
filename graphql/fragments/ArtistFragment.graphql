#import "./ImageFragment.graphql"

fragment ArtistFragment on ArtistRecord{
  id
  name
  biography(markdown: false)
  image {
    ...ImageFragment    
  }
  artwork {
    ...ImageFragment
  }
  slug
}