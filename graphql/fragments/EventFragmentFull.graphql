#import "./ImageFragment.graphql"

fragment EventFragmentFull on EventRecord {
  title
  description(markdown: false)
  startDate
  endDate
  slug
  image{
    ...ImageFragment
  }
  gallery{
   ...ImageFragment
  }
}